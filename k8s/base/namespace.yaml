# Listed first in base/kustomization.yaml so the namespace exists before
# anything is placed in it.
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    app.kubernetes.io/part-of: rdicidr
